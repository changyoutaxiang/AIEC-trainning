# 📝 AIEC 内容更新指南

## 🎯 概述

本文档介绍如何更新和修改AIEC七个习惯学习系统中的练习内容。通过标准流程，你可以轻松地从Markdown源文件更新到实际的学习界面。

## 📁 文件结构

```
新人修行/
├── 内容生产专用文件夹/           # 📝 内容源文件 (你主要编辑的地方)
│   └── 习惯一：积极主动/
│       └── 训练单元 1.1：积极主动 _ 训练单元1.1：责任感 (Ownership).md
├── frontend/content/
│   └── seven_habits.json        # 🎯 实际使用的数据文件
├── tools/                       # 🔧 转换工具
│   ├── update-scenarios.js      # 场景内容快速更新
│   └── content-converter.js     # 完整内容转换 (未来扩展用)
└── update-content.sh            # 📦 一键更新脚本
```

## 🚀 快速开始

### 1. 修改内容
编辑这个文件中的场景描述：
```
内容生产专用文件夹/习惯一：积极主动/训练单元 1.1：积极主动 _ 训练单元1.1：责任感 (Ownership).md
```

### 2. 运行更新脚本
```bash
cd /Users/wangdong/Desktop/新人修行
./update-content.sh
```

### 3. 验证更新
刷新浏览器页面：http://localhost:8000/habit-learning.html?habit=1

## 📝 详细操作流程

### Step 1: 修改源文件

在MD文件中找到要修改的练习，例如：

```markdown
#### **3.1 练习 1.1.1：我的项目交付不达预期**

* **场景设定：** 你负责的一个项目刚刚结束，但在最终的复盘会上，你的领导在团队群里公开指出："小王，这次项目最终的几项核心数据指标，离我们最初设定的目标有不小的差距。我们需要聊聊。" 群里顿时安静了下来。

* **任务要求：** 在模拟的聊天窗口中，草拟你的第一条公开回复。

* **AI 评估维度：**
  1. **直面差距 (Acknowledge the Gap):** 是否坦诚、不回避地承认了结果与目标的差距？
  2. **承担责任 (Take Responsibility):** 回复中是否明确表达了"这是我的责任"，而不是寻找客观原因或辩解？
  3. **主动担责 (Propose Action):** 是否主动提出了下一步的行动计划...
```

### Step 2: 运行更新工具

有两种方式：

#### 方式A: 使用一键脚本 (推荐)
```bash
./update-content.sh
```

#### 方式B: 直接使用Node.js工具
```bash
node tools/update-scenarios.js
```

### Step 3: 验证更新效果

1. 打开浏览器：http://localhost:8000/habit-learning.html?habit=1
2. 进入"实战模拟"模块
3. 选择你修改的练习
4. 确认"场景描述"显示更新后的内容

## 🔍 工具说明

### `update-scenarios.js` - 场景内容更新器

**功能**：
- 从MD文件中提取最新的场景描述
- 自动更新JSON文件中对应的内容
- 智能对比，只更新有变化的内容

**使用场景**：
- 修改场景描述文字
- 更新任务要求
- 调整评估维度

### `content-converter.js` - 完整内容转换器 (扩展功能)

**功能**：
- 完整解析MD文件结构
- 支持批量转换多个习惯
- 支持预览模式

**使用方法**：
```bash
node tools/content-converter.js --help      # 查看帮助
node tools/content-converter.js --habit 1   # 转换习惯1
node tools/content-converter.js --dry-run   # 预览模式
```

## ⚠️ 注意事项

### 1. MD文件格式要求
- 保持现有的标题格式：`#### **3.X 练习 1.1.X：标题**`
- 场景设定必须以 `* **场景设定：**` 开头
- 任务要求必须以 `* **任务要求：**` 开头
- 评估维度使用编号列表格式

### 2. 文件编码
- 所有文件必须使用UTF-8编码
- 避免在文件名中使用特殊字符

### 3. 内容限制
- 场景描述建议控制在200-300字内
- 任务要求保持简洁明确
- 评估维度不超过3-4个

## 🐛 常见问题

### Q: 运行脚本提示"Node.js not found"
**A**: 请先安装Node.js：
```bash
# macOS
brew install node

# 或者从官网下载: https://nodejs.org/
```

### Q: 更新后页面没有变化
**A**: 
1. 硬刷新浏览器 (Cmd+Shift+R)
2. 检查浏览器控制台是否有错误
3. 确认JSON文件是否真的被更新了

### Q: MD文件格式错误导致解析失败
**A**:
1. 检查标题格式是否正确
2. 确认字段标记 (`**场景设定：**`) 是否完整
3. 参考现有的正确格式

### Q: 想添加新的练习怎么办？
**A**: 目前需要手动添加到MD文件中，按照现有格式：
```markdown
#### **3.X 练习 1.1.X：新练习标题**
* **场景设定：** 场景描述...
* **任务要求：** 任务描述...
* **AI 评估维度：**
  1. **维度名称：** 描述...
```

## 🔄 更新历史追踪

每次运行更新工具时，JSON文件中的 `metadata` 会自动更新：
- `lastUpdated`: 最后更新日期
- `lastScenarioUpdate`: 最后场景更新时间戳

## 🚀 未来扩展

目前工具主要支持习惯1的内容更新，未来计划扩展：

1. **支持所有7个习惯的内容**
2. **批量内容验证工具**
3. **内容版本管理**
4. **自动化测试集成**

## 📞 技术支持

如果遇到问题：
1. 查看工具输出的错误信息
2. 检查文件路径和格式
3. 参考本文档的常见问题部分

---

*最后更新：2025-07-22*  
*工具版本：v1.0*