# AIEC 职场软技能系统 V2.0 内容结构适配方案

## 📋 总体概述

### 当前状况
- **前端练习逻辑**：基于硬编码的`practiceData`结构，采用4步骤固定流程
- **后端AI评分**：使用技能ID映射，调用OpenRouter API进行评分
- **内容数据**：新的V2.0 JSON结构已完成，采用难度递进的关卡设计

### 目标状态
- **动态数据加载**：前端从JSON文件读取练习内容，支持关卡制设计
- **智能AI评分**：集成V2.0的`evaluationFocus`动态评估标准
- **用户体验升级**：从"步骤式"转向"关卡式"练习体验

---

## 🎯 实施计划

### 第一阶段：数据架构适配 (预计2-3小时)

#### 任务 1.1：前端数据加载重构
**目标**：将硬编码数据转换为动态JSON加载

**具体步骤**：
1. 创建新的`DataManager`类替代硬编码的`practiceData`
2. 实现JSON文件异步加载方法
3. 适配新的数据结构（skillId: 2.1.1 -> 2.1.3）
4. 更新练习初始化逻辑

**技术要点**：
- 使用`fetch()`异步加载`content/scenarios.json`和`content/options.json`
- 保持向后兼容，避免破坏现有功能
- 添加数据验证和错误处理

#### 任务 1.2：技能选择页面升级
**目标**：支持关卡制的技能展示

**具体步骤**：
1. 修改`skills.html`页面布局，显示难度等级
2. 更新`skills.js`，从JSON动态生成技能卡片
3. 添加难度标识（基础/进阶/高阶）和学习目标展示

**设计要求**：
- 采用Apple风格的清洁设计
- 关卡进度可视化显示
- 技能卡片包含难度、描述、学习目标等信息

---

### 第二阶段：练习流程重构 (预计3-4小时)

#### 任务 2.1：练习页面逻辑更新
**目标**：从4步骤模式转向单关卡挑战模式

**具体步骤**：
1. 重构`PracticeManager`类的核心逻辑
2. 修改`showStep()`方法适配新的单场景练习
3. 更新选项显示和评分逻辑
4. 适配新的反馈显示格式

**关键改动**：
- 移除stepConfig相关逻辑
- 直接显示完整场景和4个选项
- 集成详细的选项解释和反馈

#### 任务 2.2：用户界面优化
**目标**：提供更流畅的关卡挑战体验

**具体步骤**：
1. 重设计练习页面布局（单场景 + 4选项）
2. 优化选项卡片设计，突出分数和类别
3. 改进反馈显示，包含详细解释
4. 添加关卡完成状态和进度跟踪

---

### 第三阶段：AI评分系统集成 (预计2-3小时)

#### 任务 3.1：后端AI Prompt升级
**目标**：集成V2.0的智能评估标准

**具体步骤**：
1. 更新`utils/aiPrompts.js`使用新的prompt模板
2. 集成`evaluationFocus`动态评估标准
3. 添加专家参考答案到prompt中
4. 优化AI响应解析逻辑

**技术要点**：
- 从`content/aiPrompts.json`读取prompt模板
- 动态替换模板变量（skill_name, evaluation_focus等）
- 确保AI返回标准化的JSON格式

#### 任务 3.2：评分结果展示优化
**目标**：更好地展示AI评分和反馈

**具体步骤**：
1. 适配新的AI响应格式（score, highlight, improvement_point等）
2. 优化反馈显示样式，突出亮点和改进建议
3. 添加与专家答案的对比展示

---

### 第四阶段：测试与优化 (预计1-2小时)

#### 任务 4.1：功能测试
**目标**：确保所有功能正常工作

**测试清单**：
- [ ] 技能页面正确显示关卡信息
- [ ] 练习流程顺畅运行
- [ ] 选项评分和反馈正确显示
- [ ] AI评分服务正常调用
- [ ] 数据加载错误处理

#### 任务 4.2：用户体验优化
**目标**：提升交互体验和视觉效果

**优化项目**：
- 加载状态优化
- 错误提示友好化
- 响应式设计调整
- 性能优化

---

## 📁 文件修改清单

### 需要修改的文件
```
frontend/
├── js/
│   ├── practice.js          # 🔄 重构练习逻辑
│   ├── skills.js           # 🔄 更新技能展示
│   ├── api.js              # 🔄 适配新的数据格式
│   └── dataManager.js      # ✨ 新建数据管理类
├── practice.html           # 🔄 更新练习页面布局
├── skills.html             # 🔄 更新技能选择页面
└── css/style.css           # 🔄 样式优化

backend/
├── routes/ai.js            # 🔄 更新AI评分逻辑
├── utils/aiPrompts.js      # 🔄 重构prompt生成
└── utils/dataLoader.js     # ✨ 新建内容数据加载器
```

### 需要测试的功能
- 从JSON动态加载内容数据
- 关卡制技能展示和选择
- 单场景练习流程
- AI评分和反馈显示
- 错误处理和用户提示

---

## ⚠️ 风险控制

### 向后兼容性
- 保留原有API接口，渐进式更新
- 如果JSON加载失败，回退到硬编码数据
- 逐步切换，避免功能中断

### 测试策略
- 每个阶段完成后进行功能测试
- 重点测试数据加载和AI评分链路
- 准备回滚方案

### 用户体验
- 保持简洁的操作流程 [[记忆引用：用户偏好简单技术栈]]
- 确保加载速度和响应性能
- 提供清晰的错误提示和帮助信息

---

## 🚀 实施建议

### 开发节奏
1. **第一天**：完成第一、二阶段（数据架构 + 练习流程）
2. **第二天**：完成第三、四阶段（AI集成 + 测试优化）

### 测试策略
- 每完成一个任务立即测试
- 重点验证数据流转和用户交互
- 准备好回滚到V1.0的方案

### 部署策略
- 先在本地环境完整测试
- 确认无误后推送到Git触发Zeabur自动部署
- 监控部署后的运行状态

---

💡 **核心理念**：保持用户体验的连续性，通过渐进式升级实现从"步骤式练习"到"关卡式挑战"的平滑过渡，充分发挥V2.0内容设计的"决策模拟器"价值。 